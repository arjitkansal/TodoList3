const initState = [
                    'Learn git',
                    'Learn JS',
                    'Learn KOA.js',
                    'Learn React',
                    'Make To-do List',
                  ];

const rootReducer = (state = initState, action) => {
  return state;
}

export default rootReducer;
